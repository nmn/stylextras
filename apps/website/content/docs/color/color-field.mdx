---
title: Color Field
description: "Color Field provides a color primitive for color workflows."
---

import { lazy, Suspense } from "react";

export const Example = lazy(() => import("@stylextras/ui/color-field/example"));

# Color Field

Use Color Field when a workflow needs color selection or color presentation without replacing native input semantics.

The implementation is intentionally small: visual defaults come from token variables, and the public props keep native HTML attributes available where the underlying element supports them.

## Import

```tsx
import { ColorField } from "@stylextras/ui/color-field";
```

## Basic usage

Start here when wiring the component into an interface. The demo shows the expected behavior, and the code block shows the smallest useful invocation.

<Suspense fallback={null}>
  <Example />
</Suspense>

```tsx
import { ColorField } from "@stylextras/ui/color-field";

<ColorField label="Accent color" defaultValue="#4f46e5" />;
```

## Props

The public prop types are exported from `@stylextras/ui/color-field`. Most components also accept an `sx` prop for custom styles and forward safe native HTML attributes for the underlying element.

```tsx
export type ColorFieldProps = Omit<
  BaseProps,
  "className" | "style" | "type"
> & { sx?: StyleXStyles };
```

## Customize styles

Use the `sx` prop to compose custom styles with the component's token-driven defaults.

```tsx
import * as stylex from "@stylexjs/stylex";
import { colors } from "@stylextras/ui/tokens/color";
import { spacing } from "@stylextras/ui/tokens/spacing";
import { ColorField } from "@stylextras/ui/color-field";

<ColorField aria-label="Example" sx={styles.emphasis} />;

const styles = stylex.create({
  emphasis: {
    backgroundColor: colors.bgSubtle,
    padding: spacing.md,
  },
});
```

## Complete example

This example shows the component in a fuller composition with the surrounding pieces it usually needs.

```tsx
import { ColorField } from "@stylextras/ui/color-field";

function ColorFieldExample() {
  return (
    <>
      <div>
        <ColorField aria-label="Blue" defaultValue="#2563eb" />
        <ColorField aria-label="Pink" defaultValue="#ec4899" />
        <ColorField aria-label="Orange" defaultValue="#ea580c" />
      </div>
    </>
  );
}
```

## Basic field

This pattern shows another common way to compose the component.

```tsx
import { ColorField } from "@stylextras/ui/color-field";

<ColorField label="Label" />;
```

## With native props

This pattern shows another common way to compose the component.

```tsx
<ColorField label="Required field" required disabled={false} />
```

## Accessibility notes

Always provide an accessible name for color controls, and do not rely on color alone to communicate state.

## Implementation notes

- Styles use token variables from `@stylextras/ui/tokens/*`.
- The implementation lives in `packages/ui/src/color-field/index.tsx`.
