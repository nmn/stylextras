---
title: Tree
description: "Tree provides a collection primitive for item and data interfaces."
---

import { lazy, Suspense } from "react";

export const Example = lazy(() => import("@stylextras/ui/tree/example"));

# Tree

Use Tree to present related items, actions, or records while keeping structure and styling separate from application state.

The implementation is intentionally small: visual defaults come from token variables, and the public props keep native HTML attributes available where the underlying element supports them.

## Import

```tsx
import { Tree } from "@stylextras/ui/tree";
```

## Basic usage

Start here when wiring the component into an interface. The demo shows the expected behavior, and the code block shows the smallest useful invocation.

<Suspense fallback={null}>
  <Example />
</Suspense>

```tsx
import { Tree } from "@stylextras/ui/tree";

<Tree>Example</Tree>;
```

## Props

The public prop types are exported from `@stylextras/ui/tree`. Most components also accept an `sx` prop for custom styles and forward safe native HTML attributes for the underlying element.

```tsx
export type TreeProps = Omit<BaseProps, "className" | "style"> & {
  sx?: StyleXStyles;
};
```

## Customize styles

Use the `sx` prop to compose custom styles with the component's token-driven defaults.

```tsx
import * as stylex from "@stylexjs/stylex";
import { colors } from "@stylextras/ui/tokens/color";
import { spacing } from "@stylextras/ui/tokens/spacing";
import { Tree } from "@stylextras/ui/tree";

<Tree sx={styles.emphasis}>Example</Tree>;

const styles = stylex.create({
  emphasis: {
    backgroundColor: colors.bgSubtle,
    padding: spacing.md,
  },
});
```

## Complete example

This example shows the component in a fuller composition with the surrounding pieces it usually needs.

```tsx
import { Tree } from "@stylextras/ui/tree";

function TreeExample() {
  return (
    <Tree>
      <ul>
        <li>
          tokens
          <ul>
            <li>
              color
              <ul>
                <li>brand</li>
                <li>bg</li>
                <li>fg</li>
              </ul>
            </li>
            <li>
              spacing
              <ul>
                <li>base</li>
                <li>sm</li>
                <li>lg</li>
              </ul>
            </li>
          </ul>
        </li>
        <li>
          components
          <ul>
            <li>button</li>
            <li>card</li>
            <li>dialog</li>
          </ul>
        </li>
      </ul>
    </Tree>
  );
}
```

## Accessibility notes

Provide meaningful item text, headings, and native roles where the surrounding product owns richer selection or keyboard behavior.

## Implementation notes

- Styles use token variables from `@stylextras/ui/tokens/*`.
- The implementation lives in `packages/ui/src/tree/index.tsx`.
