---
title: Kbd
description: Inline keyboard key styling based on the native kbd element.
---

import { KbdExample } from '@stylextras/ui/kbd/example';

`Kbd` is a native `kbd` wrapper for rendering keyboard shortcuts and key hints with shared tokens.

## Example

<KbdExample />

## Notes

- Renders a native `kbd`
- Useful for command hints, shortcuts, and input references
- Supports a small size scale and `sx` overrides

## Props

- `size?: 'sm' | 'md'`
- `sx?: StyleXStyles`
