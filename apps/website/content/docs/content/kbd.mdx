---
title: Kbd
description: "Kbd provides a content primitive for content surfaces."
---

import { lazy, Suspense } from "react";

export const Example = lazy(() => import("@stylextras/ui/kbd/example"));

# Kbd

Use Kbd to shape reusable content blocks while keeping typography, spacing, and surface styling token-driven.

The implementation is intentionally small: visual defaults come from token variables, and the public props keep native HTML attributes available where the underlying element supports them.

## Import

```tsx
import { Kbd } from "@stylextras/ui/kbd";
```

## Basic usage

Start here when wiring the component into an interface. The demo shows the expected behavior, and the code block shows the smallest useful invocation.

<Suspense fallback={null}>
  <Example />
</Suspense>

```tsx
import { Kbd } from "@stylextras/ui/kbd";

<Kbd>⌘K</Kbd>;
```

## Props

The public prop types are exported from `@stylextras/ui/kbd`. Most components also accept an `sx` prop for custom styles and forward safe native HTML attributes for the underlying element.

```tsx
export type KbdProps = Omit<BaseProps, "className" | "style"> & {
  sx?: StyleXStyles;
  size?: KbdSize;
};
```

## Customize styles

Use the `sx` prop to compose custom styles with the component's token-driven defaults.

```tsx
import * as stylex from "@stylexjs/stylex";
import { colors } from "@stylextras/ui/tokens/color";
import { spacing } from "@stylextras/ui/tokens/spacing";
import { Kbd } from "@stylextras/ui/kbd";

<Kbd sx={styles.emphasis}>Example</Kbd>;

const styles = stylex.create({
  emphasis: {
    backgroundColor: colors.bgSubtle,
    padding: spacing.md,
  },
});
```

## Complete example

This example shows the component in a fuller composition with the surrounding pieces it usually needs.

```tsx
import { Kbd } from "@stylextras/ui/kbd";

function KbdExample() {
  return (
    <>
      <div>
        <Kbd>cmd</Kbd>
        <Kbd>k</Kbd>
        <Kbd>cmd</Kbd>
        <Kbd>s</Kbd>
        <Kbd>shift</Kbd>
        <Kbd>enter</Kbd>
      </div>
    </>
  );
}
```

## Accessibility notes

Choose semantic children and heading levels based on the page structure around the component.

## Implementation notes

- Styles use token variables from `@stylextras/ui/tokens/*`.
- The implementation lives in `packages/ui/src/kbd/index.tsx`.
