---
title: Input Fields
description: "Input Fields provides a form primitive for form interfaces."
---

import { lazy, Suspense } from "react";

export const Example = lazy(
  () => import("@stylextras/ui/input-fields/example"),
);

# Input Fields

Use Input Fields inside forms where native input behavior, labeling, validation, and token-driven styling need to stay aligned.

The implementation is intentionally small: visual defaults come from token variables, and the public props keep native HTML attributes available where the underlying element supports them.

## Import

```tsx
import { InputFields } from "@stylextras/ui/input-fields";
```

## Basic usage

Start here when wiring the component into an interface. The demo shows the expected behavior, and the code block shows the smallest useful invocation.

<Suspense fallback={null}>
  <Example />
</Suspense>

```tsx
import { InputFields } from "@stylextras/ui/input-fields";

<InputFields>Example</InputFields>;
```

## Props

The public prop types are exported from `@stylextras/ui/input-fields`. Most components also accept an `sx` prop for custom styles and forward safe native HTML attributes for the underlying element.

```tsx
export type InputFieldsProps = Omit<BaseProps, "className" | "style"> & {
  sx?: StyleXStyles;
};
```

## Customize styles

Use the `sx` prop to compose custom styles with the component's token-driven defaults.

```tsx
import * as stylex from "@stylexjs/stylex";
import { colors } from "@stylextras/ui/tokens/color";
import { spacing } from "@stylextras/ui/tokens/spacing";
import { InputFields } from "@stylextras/ui/input-fields";

<InputFields sx={styles.emphasis}>Example</InputFields>;

const styles = stylex.create({
  emphasis: {
    backgroundColor: colors.bgSubtle,
    padding: spacing.md,
  },
});
```

## Complete example

This example shows the component in a fuller composition with the surrounding pieces it usually needs.

```tsx
import { InputFields } from "@stylextras/ui/input-fields";

function InputFieldsExample() {
  return (
    <>
      <div>
        <InputFields placeholder="Search tokens" />
        <InputFields type="email" placeholder="team@example.com" />
      </div>
    </>
  );
}
```

## Basic field

This pattern shows another common way to compose the component.

```tsx
import { InputFields } from "@stylextras/ui/input-fields";

<InputFields label="Label" />;
```

## With native props

This pattern shows another common way to compose the component.

```tsx
<InputFields label="Required field" required disabled={false} />
```

## Accessibility notes

Every interactive form control should have a label or accessible name. Preserve native attributes such as required, disabled, min, max, and aria-describedby when they apply.

## Implementation notes

- Styles use token variables from `@stylextras/ui/tokens/*`.
- The implementation lives in `packages/ui/src/input-fields/index.tsx`.
