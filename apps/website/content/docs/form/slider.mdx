---
title: Slider
description: "Slider provides a form primitive for form interfaces."
---

import { lazy, Suspense } from "react";

export const Example = lazy(() => import("@stylextras/ui/slider/example"));

# Slider

Use Slider inside forms where native input behavior, labeling, validation, and token-driven styling need to stay aligned.

The implementation is intentionally small: visual defaults come from token variables, and the public props keep native HTML attributes available where the underlying element supports them.

## Import

```tsx
import { Slider } from "@stylextras/ui/slider";
```

## Basic usage

Start here when wiring the component into an interface. The demo shows the expected behavior, and the code block shows the smallest useful invocation.

<Suspense fallback={null}>
  <Example />
</Suspense>

```tsx
import { Slider } from "@stylextras/ui/slider";

<Slider aria-label="Volume" min={0} max={100} defaultValue={40} />;
```

## Props

The public prop types are exported from `@stylextras/ui/slider`. Most components also accept an `sx` prop for custom styles and forward safe native HTML attributes for the underlying element.

```tsx
export type SliderProps = Omit<
  BaseProps,
  "className" | "size" | "style" | "type"
> & {
  sx?: StyleXStyles;
  inputSx?: StyleXStyles;
  labelSx?: StyleXStyles;
  label?: ReactNode;
  size?: SliderSize;
};
```

## Customize styles

Use the `sx` prop to compose custom styles with the component's token-driven defaults.

```tsx
import * as stylex from "@stylexjs/stylex";
import { colors } from "@stylextras/ui/tokens/color";
import { spacing } from "@stylextras/ui/tokens/spacing";
import { Slider } from "@stylextras/ui/slider";

<Slider label="Label" sx={styles.emphasis} />;

const styles = stylex.create({
  emphasis: {
    backgroundColor: colors.bgSubtle,
    padding: spacing.md,
  },
});
```

## Complete example

This example shows the component in a fuller composition with the surrounding pieces it usually needs.

```tsx
import { Slider } from "@stylextras/ui/slider";

function SliderExample() {
  return (
    <>
      <div>
        <Slider aria-label="Value 25" min={0} max={100} defaultValue={25} />
        <Slider aria-label="Value 50" min={0} max={100} defaultValue={50} />
        <Slider aria-label="Value 75" min={0} max={100} defaultValue={75} />
      </div>
    </>
  );
}
```

## Basic field

This pattern shows another common way to compose the component.

```tsx
import { Slider } from "@stylextras/ui/slider";

<Slider label="Label" />;
```

## With native props

This pattern shows another common way to compose the component.

```tsx
<Slider label="Required field" required disabled={false} />
```

## Accessibility notes

Every interactive form control should have a label or accessible name. Preserve native attributes such as required, disabled, min, max, and aria-describedby when they apply.

## Implementation notes

- Styles use token variables from `@stylextras/ui/tokens/*`.
- The implementation lives in `packages/ui/src/slider/index.tsx`.
