---
title: Button
description: Stylextras wrapper component.
---

Wraps `aria.Button` from `react-aria-components`, omitting `className` and `style` and adding `style?: StyleXStyles`.

## Demo

<AriaButton> Click me </AriaButton>

```tsx
import { Button } from '@/components/stylextras';

<Button />
```

## Props
```ts
import type { ButtonProps } from '@/components/stylextras';
```
