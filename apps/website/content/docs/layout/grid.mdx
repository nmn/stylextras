---
title: Grid
description: "Grid provides a layout primitive for layout composition."
---

import { lazy, Suspense } from "react";

export const Example = lazy(() => import("@stylextras/ui/grid/example"));

# Grid

Use Grid to compose page structure without leaking layout-specific CSS into feature components.

The implementation is intentionally small: visual defaults come from token variables, and the public props keep native HTML attributes available where the underlying element supports them.

## Import

```tsx
import { Grid } from "@stylextras/ui/grid";
```

## Basic usage

Start here when wiring the component into an interface. The demo shows the expected behavior, and the code block shows the smallest useful invocation.

<Suspense fallback={null}>
  <Example />
</Suspense>

```tsx
import { Grid } from "@stylextras/ui/grid";

<Grid>Example</Grid>;
```

## Props

The public prop types are exported from `@stylextras/ui/grid`. Most components also accept an `sx` prop for custom styles and forward safe native HTML attributes for the underlying element.

```tsx
export type GridProps = Omit<BaseProps, "className" | "style"> & {
  sx?: StyleXStyles;
  cols?: GridCols;
};
```

## Customize styles

Use the `sx` prop to compose custom styles with the component's token-driven defaults.

```tsx
import * as stylex from "@stylexjs/stylex";
import { colors } from "@stylextras/ui/tokens/color";
import { spacing } from "@stylextras/ui/tokens/spacing";
import { Grid } from "@stylextras/ui/grid";

<Grid sx={styles.emphasis}>Example</Grid>;

const styles = stylex.create({
  emphasis: {
    backgroundColor: colors.bgSubtle,
    padding: spacing.md,
  },
});
```

## Complete example

This example shows the component in a fuller composition with the surrounding pieces it usually needs.

```tsx
import { Grid } from "@stylextras/ui/grid";

function GridExample() {
  return (
    <>
      <div>
        <Grid cols={2}>
          <div>One</div>
          <div>Two</div>
        </Grid>
        <Grid cols={3}>
          <div>One</div>
          <div>Two</div>
          <div>Three</div>
        </Grid>
        <Grid cols={4}>
          <div>One</div>
          <div>Two</div>
          <div>Three</div>
          <div>Four</div>
        </Grid>
      </div>
    </>
  );
}
```

## Grouped composition

Group related items when the collection should scan as a single unit.

```tsx
import { Grid } from "@stylextras/ui/grid";

<Grid>
  <Button>One</Button>
  <Button variant="secondary">Two</Button>
</Grid>;
```

## Accessibility notes

Layout components should not replace semantic landmarks. Use nav, main, aside, section, and heading elements where they describe the content.

## Implementation notes

- Styles use token variables from `@stylextras/ui/tokens/*`.
- The implementation lives in `packages/ui/src/grid/index.tsx`.
