---
title: "ColorField"
description: "Editable color value."
---

import Example from "@stylextras/ui/experimental/color-field/example";
import exampleCode from "@stylextras/ui/experimental/color-field/example?raw";

This component is experimental. Its API and behavior may change before it moves into the stable catalog.

## Examples

<ComponentPreview code={exampleCode} name="ColorField">
  <Example />
</ComponentPreview>

## Usage

The live example above is the complete, type-checked package example. It shows the current experimental composition and representative states; copy its displayed source as a starting point, then consult the API reference before depending on the contract.

## Import

```tsx
import { ColorField } from "@stylextras/ui/experimental/color-field";
```

## Anatomy

- `ColorField` — Native `<input type="color">`.

## Accessibility and browser behavior

`ColorField` is a zero-JavaScript native color input. The browser supplies its picker, keyboard behavior, validation, and form participation. Give the input a visible label or an accessible name.

The component adds a visible focus outline, invalid-state border, forced-colors border, disabled treatment, and minimum pointer target. Any native input prop not named in `Omit` remains accepted, including standard ARIA/data attributes, event handlers, and `ref`. `sx` is composed last.

## API reference

### ColorField

`ColorFieldProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"input">, "className" | "style" | "type">`. The props below complete `ColorFieldProps`.

- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.
