---
title: "TagList"
description: "Semantic list for static tags."
---

import Example from "@stylextras/ui/experimental/tag-group/example";

This component is experimental. Its API and behavior may change before it moves into the stable catalog.

## Examples

<ComponentPreview name="TagGroup">
  <Example />
</ComponentPreview>

### Example code

```tsx
import { Badge } from '../badge';
import { TagItem, TagList } from './index';

export default function Example() {
  return (
    <TagList aria-label="Component metadata">
      <TagItem><Badge>Design system</Badge></TagItem>
      <TagItem><Badge>Experimental</Badge></TagItem>
      <TagItem><Badge>Native popup</Badge></TagItem>
    </TagList>
  );
}
```

## Import

```tsx
import { TagItem, TagList } from "@stylextras/ui/experimental/tag-group";
```

## Anatomy

- `TagList` — `<ul role="list">` preserving markerless-list semantics.
- `TagItem` — Explicit `<li>` for one static tag.

## Accessibility and browser behavior

`TagList` and `TagItem` preserve native list semantics without JavaScript. The
explicit item contract prevents children from being silently wrapped and keeps
refs and item props on the actual `<li>`. Use native checkbox or radio controls
when tags are selectable, and buttons when each tag performs an action.

Native `ul` props, including accessible naming, standard ARIA/data attributes, event handlers, and `ref`, remain accepted. `TagGroup` remains as a deprecated compatibility alias. `sx` is composed last.

## API reference

### TagList

`TagListProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"ul">, "className" | "style">`. The props below complete `TagListProps`.

- `sx` — `stylex.StyleXStyles`.

### TagItem

`TagItemProps` extends native `<li>` props, accepts a forwarded ref, and adds `sx`.

### TagGroup (deprecated alias)

`TagGroupProps`

`TagGroup` aliases `TagList`, and `TagGroupProps` is the same type as `TagListProps`. Prefer the list name because these tags are static metadata, not interactive group controls.
