---
title: "ColorSwatchPicker"
description: "Color sample collection."
---

import Example from "@stylextras/ui/experimental/color-swatch-picker/example";
import exampleCode from "@stylextras/ui/experimental/color-swatch-picker/example?raw";

This component is experimental. Its API and behavior may change before it moves into the stable catalog.

## Examples

<ComponentPreview code={exampleCode} name="ColorSwatchPicker">
  <Example />
</ComponentPreview>

## Usage

The live example above is the complete, type-checked package example. It shows the current experimental composition and representative states; copy its displayed source as a starting point, then consult the API reference before depending on the contract.

## Import

```tsx
import { ColorSwatchPicker } from "@stylextras/ui/experimental/color-swatch-picker";
```

## Anatomy

- `ColorSwatchPicker` — Native `<fieldset>` containing one radio input per color.

## Accessibility and browser behavior

Each part renders and styles the native element named above. The API reference states the exact React or inherited prop base, including whether `ref` is accepted, every omitted native prop, and every component-applied default. Any native prop not named in `Omit` remains accepted, including standard ARIA/data attributes and event handlers. `sx` is composed last.

Native radio inputs provide arrow-key selection, focus, validation, and form
submission. The visible legend names the group, and each swatch has a text label
in addition to its color. Options may provide their own submitted `value`,
visible `label`, disabled state, and per-input props. Duplicate submitted values
are ignored with a development warning.

## API reference

### ColorSwatchPicker

`ColorSwatchPickerProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"fieldset">, "className" | "defaultValue" | "style">`. The props below complete `ColorSwatchPickerProps`.

- `colors` — `Array<string | ColorSwatchPickerOption>`. Ordered swatches, optionally including per-option labels and disabled state. Default: built-in colors.
- `defaultValue` — `string`. Initial value for uncontrolled usage.
- `getColorLabel` — `(color: string, index: number) => ReactNode`. Produces an accessible label when an option does not provide one.
- `legend` — `ReactNode`. Visible accessible label for the grouped controls. Required.
- `name` — `string`. Native form field name used during submission. Required.
- `required` — `boolean`. Participates in native required-value validation. Default: `false`.
- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.
