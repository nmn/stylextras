---
title: "RangeCalendar"
description: "Date-range calendar grid."
---

import Example from "@stylextras/ui/experimental/range-calendar/example";
import exampleCode from "@stylextras/ui/experimental/range-calendar/example?raw";

This component is experimental. Its API and behavior may change before it moves into the stable catalog.

## Examples

<ComponentPreview code={exampleCode} name="RangeCalendar">
  <Example />
</ComponentPreview>

## Usage

The live example above is the complete, type-checked package example. It shows the current experimental composition and representative states; copy its displayed source as a starting point, then consult the API reference before depending on the contract.

## Import

```tsx
import { RangeCalendar } from "@stylextras/ui/experimental/range-calendar";
```

## Anatomy

- `RangeCalendar` — Native `<fieldset>` with two labeled date inputs.

## Accessibility and browser behavior

Each part renders and styles the native element named above. The API reference states the exact React or inherited prop base, including whether `ref` is accepted, every omitted native prop, and every component-applied default. Any native prop not named in `Omit` remains accepted, including standard ARIA/data attributes and event handlers. `sx` is composed last.

The paired native date controls preserve platform entry, constraints, external
form association, validation, reset, and submission without JavaScript. The
legend names the range and each field retains its own visible label. Use
`startProps` and `endProps` for field-specific descriptions, native attributes,
event handlers, and refs while the range owns IDs, names, values, and bounds.

## API reference

### RangeCalendar

`RangeCalendarProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"fieldset">, "className" | "style">`. The props below complete `RangeCalendarProps`.

- `legend` — `ReactNode`. Visible accessible label for the grouped controls. Required.
- `startLabel`, `endLabel` — `ReactNode`.
- `startName`, `endName` — `string`.
- `startDefaultValue`, `endDefaultValue` — `string`.
- `startProps`, `endProps` — Per-field `DateField` props not owned by the range wrapper.
- `min`, `max` — ISO date strings.
- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.
