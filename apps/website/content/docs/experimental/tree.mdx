---
title: "Tree"
description: "Hierarchical interactive collection."
---

import Example from "@stylextras/ui/experimental/tree/example";
import exampleCode from "@stylextras/ui/experimental/tree/example?raw";

This component is experimental. Its API and behavior may change before it moves into the stable catalog.

## Examples

<ComponentPreview code={exampleCode} name="Tree">
  <Example />
</ComponentPreview>

## Usage

The live example above is the complete, type-checked package example. It shows the current experimental composition and representative states; copy its displayed source as a starting point, then consult the API reference before depending on the contract.

## Import

```tsx
import { Tree, TreeBranch, TreeBranchContent, TreeBranchTrigger, TreeGroup, TreeItem } from "@stylextras/ui/experimental/tree";
```

## Anatomy

- `Tree` — Styled `<div>`.
- `TreeBranch` — Styled `<details>`.
- `TreeBranchContent` — Styled `<ul>`.
- `TreeBranchTrigger` — Styled `<summary>`.
- `TreeGroup` — Styled `<ul>`.
- `TreeItem` — Styled `<li>`.

## Accessibility and browser behavior

Each part renders and styles the native element named above. The API reference states the exact React or inherited prop base, including whether `ref` is accepted, every omitted native prop, and every component-applied default. Any native prop not named in `Omit` remains accepted, including standard ARIA/data attributes and event handlers. `sx` is composed last.

This is a native nested disclosure list, not an ARIA tree view. Summary elements provide disclosure keyboard behavior without a controller or focusgroup polyfill.

## API reference

### Tree

`TreeProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"div">, "className" | "role" | "style">`. The props below complete `TreeProps`.

- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.

### TreeBranch

`TreeBranchProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"details">, "className" | "style">`. The props below complete `TreeBranchProps`.

- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.

### TreeBranchContent

`TreeBranchContentProps`

Complete native/inherited prop base: `TreeGroupProps`. The props below complete `TreeBranchContentProps`.

- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.

### TreeBranchTrigger

`TreeBranchTriggerProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"summary">, "className" | "style">`. The props below complete `TreeBranchTriggerProps`.

- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.

### TreeGroup

`TreeGroupProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"ul">, "className" | "style">`. The props below complete `TreeGroupProps`.

- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.

### TreeItem

`TreeItemProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"li">, "className" | "style">`. The props below complete `TreeItemProps`.

- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.
