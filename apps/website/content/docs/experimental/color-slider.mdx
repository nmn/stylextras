---
title: "ColorSlider"
description: "Color-channel slider."
---

import Example from "@stylextras/ui/experimental/color-slider/example";
import exampleCode from "@stylextras/ui/experimental/color-slider/example?raw";

This component is experimental. Its API and behavior may change before it moves into the stable catalog.

## Examples

<ComponentPreview code={exampleCode} name="ColorSlider">
  <Example />
</ComponentPreview>

## Usage

The live example above is the complete, type-checked package example. It shows the current experimental composition and representative states; copy its displayed source as a starting point, then consult the API reference before depending on the contract.

## Import

```tsx
import { ColorSlider } from "@stylextras/ui/experimental/color-slider";
```

## Anatomy

- `ColorSlider` — Native `<input type="range">`.

## Accessibility and browser behavior

`ColorSlider` is a zero-JavaScript native range input. The browser supplies keyboard, pointer, validation, and form behavior. Give each slider a visible label or accessible name that identifies its channel, and provide `aria-valuetext` when the number alone does not describe the color value.

The component adds a visible focus outline and minimum pointer target. Any native input prop not named in `Omit` remains accepted, including standard ARIA/data attributes, event handlers, and `ref`. `sx` is composed last.

## API reference

### ColorSlider

`ColorSliderProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"input">, "className" | "style" | "type">`. The props below complete `ColorSliderProps`.

- `max` — `ColorSliderProps["max"]`. Inclusive upper bound for the native color range. Default: `360`.
- `min` — `ColorSliderProps["min"]`. Inclusive lower bound for the native color range. Default: `0`.
- `step` — `ColorSliderProps["step"]`. Sets the permitted increment between numeric values. Default: `1`.
- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.
