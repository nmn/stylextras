---
title: "EditableText"
description: "Inline text editor."
---

import Example from "@stylextras/ui/experimental/editable-text/example";
import exampleCode from "@stylextras/ui/experimental/editable-text/example?raw";

This component is experimental. Its API and behavior may change before it moves into the stable catalog.

## Examples

<ComponentPreview code={exampleCode} name="EditableText">
  <Example />
</ComponentPreview>

## Usage

The live example above is the complete, type-checked package example. It shows the current experimental composition and representative states; copy its displayed source as a starting point, then consult the API reference before depending on the contract.

## Import

```tsx
import { EditableText } from "@stylextras/ui/experimental/editable-text";
```

## Anatomy

- `EditableText` — Native `<input type="text">` or `<textarea>`.

## Accessibility and browser behavior

Each part renders and styles the native element named above. The API reference states the exact React or inherited prop base, including whether `ref` is accepted, every omitted native prop, and every component-applied default. Any native prop not named in `Omit` remains accepted, including standard ARIA/data attributes and event handlers. `sx` is composed last.

The component uses native form controls instead of `contenteditable`, preserving labeling, selection, paste, validation, reset, and assistive-technology behavior without component JavaScript.

## API reference

### EditableText

`EditableTextProps`

Complete native/inherited prop base: native input props when `multiline` is false, or native textarea props when `multiline` is true. `className`, `style`, and input `type` are component-owned.

- `multiline` — `boolean`. Renders a textarea instead of a single-line text input. Default: `false`.
- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.
