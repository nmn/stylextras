---
title: "ColorPicker"
description: "Composed color editing tool."
---

import Example from "@stylextras/ui/experimental/color-picker/example";

This component is experimental. Its API and behavior may change before it moves into the stable catalog.

## Examples

<ComponentPreview name="ColorPicker">
  <Example />
</ComponentPreview>

### Example code

```tsx
'use client';

import { ColorPicker } from './index';

export default function Example() {
  return (
    <>
      <ColorPicker aria-label="Brand" defaultValue="#0f766e" />
      <ColorPicker aria-label="Info" defaultValue="#2563eb" />
    </>
  );
}
```

## Import

```tsx
import { ColorPicker } from "@stylextras/ui/experimental/color-picker";
```

## Anatomy

- `ColorPicker` — Styled `<div>`.

## Accessibility and browser behavior

Each part renders and styles the native element named above. The API reference states the exact React or inherited prop base, including whether `ref` is accepted, every omitted native prop, and every component-applied default. Any native prop not named in `Omit` remains accepted, including standard ARIA/data attributes and event handlers. `sx` is composed last.

This component uses a focused client controller for composite state and keyboard interaction while retaining native elements for focus and form semantics where possible. Its behavior comes from the component controller rather than a browser-feature polyfill.

## API reference

### ColorPicker

`ColorPickerProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithoutRef<"div">, "className" | "style">`. The props below complete `ColorPickerProps`.

- `sx` — `stylex.StyleXStyles`.
