---
title: Navbar
description: "Navbar provides a navigation primitive for navigation interfaces."
---

import { lazy, Suspense } from "react";

export const Example = lazy(() => import("@stylextras/ui/navbar/example"));

# Navbar

Use Navbar for navigation patterns that need predictable structure, token-driven spacing, and clear focus behavior.

The implementation is intentionally small: visual defaults come from token variables, and the public props keep native HTML attributes available where the underlying element supports them.

## Import

```tsx
import { Navbar } from "@stylextras/ui/navbar";
```

## Basic usage

Start here when wiring the component into an interface. The demo shows the expected behavior, and the code block shows the smallest useful invocation.

<Suspense fallback={null}>
  <Example />
</Suspense>

```tsx
import { Navbar } from "@stylextras/ui/navbar";

<Navbar>Example</Navbar>;
```

## Props

The public prop types are exported from `@stylextras/ui/navbar`. Most components also accept an `sx` prop for custom styles and forward safe native HTML attributes for the underlying element.

```tsx
export type NavbarProps = Omit<BaseProps, "className" | "style"> & {
  sx?: StyleXStyles;
};
```

## Customize styles

Use the `sx` prop to compose custom styles with the component's token-driven defaults.

```tsx
import * as stylex from "@stylexjs/stylex";
import { colors } from "@stylextras/ui/tokens/color";
import { spacing } from "@stylextras/ui/tokens/spacing";
import { Navbar } from "@stylextras/ui/navbar";

<Navbar sx={styles.emphasis}>Example</Navbar>;

const styles = stylex.create({
  emphasis: {
    backgroundColor: colors.bgSubtle,
    padding: spacing.md,
  },
});
```

## Complete example

This example shows the component in a fuller composition with the surrounding pieces it usually needs.

```tsx
import { Navbar } from "@stylextras/ui/navbar";
import { Link } from "@stylextras/ui/link";

function NavbarExample() {
  return (
    <>
      <Navbar>
        <Link href="#docs">Docs</Link>
        <Link href="#components">Components</Link>
        <Link href="#themes">Themes</Link>
      </Navbar>
    </>
  );
}
```

## Accessibility notes

Use real links for navigation, mark the current location where appropriate, and keep focus order consistent with visual order.

## Implementation notes

- Styles use token variables from `@stylextras/ui/tokens/*`.
- The implementation lives in `packages/ui/src/navbar/index.tsx`.
