---
title: Timefield
description: "Timefield provides a date and time primitive for date and time workflows."
---

import { lazy, Suspense } from "react";

export const Example = lazy(() => import("@stylextras/ui/timefield/example"));

# Timefield

Use Timefield for date or time entry flows that can lean on native form controls and browser validation.

The implementation is intentionally small: visual defaults come from token variables, and the public props keep native HTML attributes available where the underlying element supports them.

## Import

```tsx
import { Timefield } from "@stylextras/ui/timefield";
```

## Basic usage

Start here when wiring the component into an interface. The demo shows the expected behavior, and the code block shows the smallest useful invocation.

<Suspense fallback={null}>
  <Example />
</Suspense>

```tsx
import { Timefield } from "@stylextras/ui/timefield";

<Timefield aria-label="Start time" defaultValue="09:30" />;
```

## Props

The public prop types are exported from `@stylextras/ui/timefield`. Most components also accept an `sx` prop for custom styles and forward safe native HTML attributes for the underlying element.

```tsx
export type TimefieldProps = Omit<BaseProps, "className" | "style" | "type"> & {
  sx?: StyleXStyles;
};
```

## Customize styles

Use the `sx` prop to compose custom styles with the component's token-driven defaults.

```tsx
import * as stylex from "@stylexjs/stylex";
import { colors } from "@stylextras/ui/tokens/color";
import { spacing } from "@stylextras/ui/tokens/spacing";
import { Timefield } from "@stylextras/ui/timefield";

<Timefield aria-label="Example" sx={styles.emphasis} />;

const styles = stylex.create({
  emphasis: {
    backgroundColor: colors.bgSubtle,
    padding: spacing.md,
  },
});
```

## Complete example

This example shows the component in a fuller composition with the surrounding pieces it usually needs.

```tsx
import { Timefield } from "@stylextras/ui/timefield";

function TimefieldExample() {
  return (
    <>
      <div>
        <Timefield aria-label="Start time" defaultValue="09:30" />
        <Timefield aria-label="End time" defaultValue="17:30" />
      </div>
    </>
  );
}
```

## Basic field

This pattern shows another common way to compose the component.

```tsx
import { Timefield } from "@stylextras/ui/timefield";

<Timefield label="Label" />;
```

## With native props

This pattern shows another common way to compose the component.

```tsx
<Timefield label="Required field" required disabled={false} />
```

## Accessibility notes

Provide an accessible label and keep helper or validation text close to the field that it describes.

## Implementation notes

- Styles use token variables from `@stylextras/ui/tokens/*`.
- The implementation lives in `packages/ui/src/timefield/index.tsx`.
