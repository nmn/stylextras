---
title: Calendar
description: "Calendar provides a date and time primitive for date and time workflows."
---

import { lazy, Suspense } from "react";

export const Example = lazy(() => import("@stylextras/ui/calendar/example"));

# Calendar

Use Calendar for date or time entry flows that can lean on native form controls and browser validation.

The implementation is intentionally small: visual defaults come from token variables, and the public props keep native HTML attributes available where the underlying element supports them.

## Import

```tsx
import { Calendar } from "@stylextras/ui/calendar";
```

## Basic usage

Start here when wiring the component into an interface. The demo shows the expected behavior, and the code block shows the smallest useful invocation.

<Suspense fallback={null}>
  <Example />
</Suspense>

```tsx
import { Calendar } from "@stylextras/ui/calendar";

<Calendar aria-label="Start date" defaultValue="2026-04-10" />;
```

## Props

The public prop types are exported from `@stylextras/ui/calendar`. Most components also accept an `sx` prop for custom styles and forward safe native HTML attributes for the underlying element.

```tsx
export type CalendarProps = Omit<BaseProps, "className" | "style" | "type"> & {
  sx?: StyleXStyles;
};
```

## Customize styles

Use the `sx` prop to compose custom styles with the component's token-driven defaults.

```tsx
import * as stylex from "@stylexjs/stylex";
import { colors } from "@stylextras/ui/tokens/color";
import { spacing } from "@stylextras/ui/tokens/spacing";
import { Calendar } from "@stylextras/ui/calendar";

<Calendar aria-label="Example" sx={styles.emphasis} />;

const styles = stylex.create({
  emphasis: {
    backgroundColor: colors.bgSubtle,
    padding: spacing.md,
  },
});
```

## Complete example

This example shows the component in a fuller composition with the surrounding pieces it usually needs.

```tsx
import { Calendar } from "@stylextras/ui/calendar";

function CalendarExample() {
  return (
    <>
      <div>
        <Calendar aria-label="Start date" defaultValue="2026-04-10" />
        <Calendar aria-label="Review date" defaultValue="2026-04-18" />
      </div>
    </>
  );
}
```

## Accessibility notes

Provide an accessible label and keep helper or validation text close to the field that it describes.

## Implementation notes

- Styles use token variables from `@stylextras/ui/tokens/*`.
- The implementation lives in `packages/ui/src/calendar/index.tsx`.
