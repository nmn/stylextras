---
title: "Progress"
description: "Styled native progress element."
---

import Example from "@stylextras/ui/progress/example";

## Examples

<ComponentPreview name="Progress">
  <Example />
</ComponentPreview>

### Example code

```tsx
import { Label } from '../label';
import { Progress } from './index';

export default function Example() {
  return (
    <>
      <Label htmlFor="tokens-progress">Token migration · 82%</Label>
      <Progress id="tokens-progress" value={82} />
      <Label htmlFor="components-progress">Component migration · 64%</Label>
      <Progress id="components-progress" value={64} />
      <Label htmlFor="indeterminate-progress">Publishing package</Label>
      <Progress id="indeterminate-progress" />
    </>
  );
}
('use client');
```

## Import

```tsx
import { Progress } from "@stylextras/ui/progress";
```

## Anatomy

- `Progress` — Styled `<progress>`.

## Accessibility and browser behavior

Each part renders and styles the native element named above. The API reference states the exact React or inherited prop base, including whether `ref` is accepted, every omitted native prop, and every component-applied default. Any native prop not named in `Omit` remains accepted, including standard ARIA/data attributes and event handlers. `sx` is composed last.

This component relies on the browser for its behavior, including keyboard, focus, form, and validation semantics where those apply. It does not ship a component behavior controller. If an optional presentation API is unavailable, the underlying native control and semantics continue to work with the browser’s ordinary presentation.

## API reference

### Progress

`ProgressProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"progress">, "className" | "style">`. The props below complete `ProgressProps`.

- `max` — `ProgressProps["max"]`. Default: `100`.
- `sx` — `stylex.StyleXStyles`.
