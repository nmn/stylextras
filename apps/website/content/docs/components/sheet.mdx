---
title: "Sheet"
description: "Side-aligned native dialog."
---

import Example from "@stylextras/ui/sheet/example";
import exampleCode from "@stylextras/ui/sheet/example?raw";

## Examples

<ComponentPreview code={exampleCode} name="Sheet">
  <Example />
</ComponentPreview>

## Usage

The live example above is the complete, type-checked package example. It shows the recommended composition and representative variants or states; copy its displayed source as a starting point, then keep only the parts your interface needs.

## Import

```tsx
import { Sheet, SheetBody, SheetClose, SheetFooter, SheetHeader, SheetTrigger } from "@stylextras/ui/sheet";
import { DialogCommandBridge } from "@stylextras/ui/dialog/client";
```

## Anatomy

- `Sheet` — Styled `<dialog>`.
- `SheetBody` — Styled `<div>`.
- `SheetClose` — Styled `<button>`.
- `SheetFooter` — Styled `<div>`.
- `SheetHeader` — Styled `<div>`.
- `SheetTrigger` — Styled `<button>`.
- `DialogCommandBridge` — Optional feature-detected invoker-command fallback.

## Accessibility and browser behavior

Each part renders and styles the native element named above. The API reference states the exact React or inherited prop base, including whether `ref` is accepted, every omitted native prop, and every component-applied default. Any native prop not named in `Omit` remains accepted, including standard ARIA/data attributes and event handlers. `sx` is composed last.

Give the native dialog a heading relationship and associate concise descriptive
copy when it helps explain the task. The example installs
`DialogCommandBridge`, which feature-detects native invoker commands before
loading its fallback. `LazySheet` installs the same fallback internally.

## API reference

### Sheet

`SheetProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"dialog">, "aria-label" | "aria-labelledby" | "className" | "style">` plus one required accessible name.

- `aria-label` or `aria-labelledby` — Required accessible dialog name.
- `side` — `SheetSide`. Selects the edge from which the surface is positioned. Default: `'end'`.
- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.

### SheetBody

`SheetBodyProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"div">, "className" | "style">`. The props below complete `SheetBodyProps`.

- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.

### SheetClose

`SheetCloseProps`

Complete native/inherited prop base: `SheetTriggerProps`. The props below complete `SheetCloseProps`.

- `size` — `ButtonSize`. Controls visual dimensions and, for interactive components, the target size. Default: `'md'`.
- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.
- `target` — `string`. ID of the element controlled by this trigger. Required.
- `type` — `SheetCloseProps["type"]`. Sets the underlying element's native control type. Default: `'button'`.
- `variant` — `ButtonVariant`. Selects the built-in visual treatment. Default: `'outline'`.

### SheetFooter

`SheetFooterProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"div">, "className" | "style">`. The props below complete `SheetFooterProps`.

- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.

### SheetHeader

`SheetHeaderProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"div">, "className" | "style">`. The props below complete `SheetHeaderProps`.

- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.

### SheetTrigger

`SheetTriggerProps`

Complete native/inherited prop base: `ButtonProps`. The props below complete `SheetTriggerProps`.

- `size` — `ButtonSize`. Controls visual dimensions and, for interactive components, the target size. Default: `'md'`.
- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.
- `target` — `string`. ID of the element controlled by this trigger. Required.
- `type` — `SheetTriggerProps["type"]`. Sets the underlying element's native control type. Default: `'button'`.
- `variant` — `ButtonVariant`. Selects the built-in visual treatment. Default: `'primary'`.
