---
title: "Carousel"
description: "Scroll-snap carousel with progressive controls."
---

import Example from "@stylextras/ui/carousel/example";

## Examples

<ComponentPreview name="Carousel">
  <Example />
</ComponentPreview>

### Example code

```tsx
import * as stylex from '@stylexjs/stylex';
import {
  Card,
  CardContent,
  CardDescription,
  CardHeader,
  CardTitle,
} from '../card';
import { colors } from '../tokens/color.stylex';
import { typography } from '../tokens/typography.stylex';
import { Carousel, CarouselItem } from './index';

const slides = [
  ['Native controls', 'Scroll snapping supplies the core interaction.'],
  [
    'Progressive markers',
    'Supporting browsers add scroll markers and buttons.',
  ],
  ['Reduced motion', 'Smooth scrolling yields to the user motion preference.'],
  [
    'Anchor positioning',
    'Layered controls stay attached without measurement code.',
  ],
  [
    'Popover API',
    'Light dismiss and top-layer rendering come from the platform.',
  ],
  [
    'Dialog commands',
    'Explicit invoker targets keep modal triggers declarative.',
  ],
  [
    'Focus groups',
    'Arrow-key navigation progressively enhances grouped controls.',
  ],
  [
    'View transitions',
    'Optional transitions add continuity without owning navigation.',
  ],
  [
    'Container queries',
    'Components respond to available space instead of the viewport.',
  ],
  [
    'StyleX themes',
    'Independent variable sets compose directly on any ancestor.',
  ],
];

export default function Example() {
  return (
    <Carousel
      aria-label="Browser API highlights"
      previousButtonLabel="Previous highlight"
      nextButtonLabel="Next highlight"
    >
      {slides.map(([title, description], index) => (
        <CarouselItem
          key={title}
          aria-label={`${index + 1} of ${slides.length}: ${title}`}
          sx={styles.slide}
        >
          <Card sx={styles.card}>
            <CardHeader>
              <span {...stylex.props(styles.number)}>
                {String(index + 1).padStart(2, '0')}
              </span>
              <CardTitle>{title}</CardTitle>
              <CardDescription>{description}</CardDescription>
            </CardHeader>
            <CardContent>
              <span {...stylex.props(styles.meta)}>Scroll snap card</span>
            </CardContent>
          </Card>
        </CarouselItem>
      ))}
    </Carousel>
  );
}

const styles = stylex.create({
  slide: {
    borderWidth: 0,
  },
  card: {
    height: '100%',
    minHeight: '12rem',
  },
  number: {
    color: colors.fgMuted,
    fontFamily: typography.fontMono,
    fontSize: typography.stepMinus1,
  },
  meta: {
    color: colors.fgMuted,
    fontFamily: typography.fontMono,
    fontSize: typography.stepMinus2,
    textTransform: 'uppercase',
  },
});
```

## Import

```tsx
import { Carousel, CarouselItem } from "@stylextras/ui/carousel";
```

## Anatomy

- `Carousel` — Styled `<div>`.
- `CarouselItem` — Named `<div role="group" aria-roledescription="slide">`.

## Accessibility and browser behavior

Each part renders and styles the native element named above. The API reference states the exact React or inherited prop base, including whether `ref` is accepted, every omitted native prop, and every component-applied default. Any native prop not named in `Omit` remains accepted, including standard ARIA/data attributes and event handlers. `sx` is composed last.

The root and every slide require accessible names. Current Chromium can expose
CSS scroll buttons and markers; their localized names come from generated
content alternative text. Other engines retain a keyboard-focusable native
scroll region with scroll snapping. Root roles are restricted to `region` or
`group`, and slide roles to `group`.

## API reference

### Carousel

`CarouselProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"div">, "aria-label" | "aria-labelledby" | "className" | "role" | "style">` plus one required accessible name.

- `role` — `CarouselProps["role"]`. Default: `'region'`.
- `previousButtonLabel` — `string`. Default: `'Previous slide'`.
- `nextButtonLabel` — `string`. Default: `'Next slide'`.
- `sx` — `stylex.StyleXStyles`.
- `tabIndex` — `CarouselProps["tabIndex"]`. Default: `0`.

### CarouselItem

`CarouselItemProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"div">, "aria-label" | "aria-labelledby" | "className" | "role" | "style">` plus one required accessible name.

- `aria-label` or `aria-labelledby` — Required slide name.
- `role` — `'group'`. Default: `'group'`.
- `sx` — `stylex.StyleXStyles`.
