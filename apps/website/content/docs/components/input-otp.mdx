---
title: "InputOTP"
description: "One-time-code input controller."
---

import Example from "@stylextras/ui/input-otp/example";
import exampleCode from "@stylextras/ui/input-otp/example?raw";

## Examples

<ComponentPreview code={exampleCode} name="InputOTP">
  <Example />
</ComponentPreview>

## Usage

The live example above is the complete, type-checked package example. It shows the recommended composition and representative variants or states; copy its displayed source as a starting point, then keep only the parts your interface needs.

## Import

```tsx
import { InputOTP } from "@stylextras/ui/input-otp";
```

## Anatomy

- `InputOTP` — Styled `<input>`.

## Accessibility and browser behavior

Each part renders and styles the native element named above. The API reference states the exact React or inherited prop base, including whether `ref` is accepted, every omitted native prop, and every component-applied default. Any native prop not named in `Omit` remains accepted, including standard ARIA/data attributes and event handlers. `sx` is composed last.

This component uses a focused client controller for composite state and keyboard interaction while retaining native elements for focus and form semantics where possible. Its behavior comes from the component controller rather than a browser-feature polyfill.

## API reference

### InputOTP

`InputOTPProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"input">, "className" | "inputMode" | "style" | "type">`. The props below complete `InputOTPProps`.

- `autoComplete` — `InputOTPProps["autoComplete"]`. Provides the native browser autofill hint. Default: `'one-time-code'`.
- `length` — `number`. Default: `6`. Number of characters accepted.
- `maxLength` — `InputOTPProps["maxLength"]`. Native maximum length kept in sync with the rendered cell count. Default: `length`.
- `minLength` — `InputOTPProps["minLength"]`. Native minimum length kept in sync with the rendered cell count and validity model. Default: `length`.
- `pattern` — `InputOTPProps["pattern"]`. Native validation pattern for accepted one-time-code characters. Default: `'[0-9]*'`.
- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.
