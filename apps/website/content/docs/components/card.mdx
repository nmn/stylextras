---
title: "Card"
description: "Composed content surface."
---

import Example from "@stylextras/ui/card/example";
import exampleCode from "@stylextras/ui/card/example?raw";

## Examples

<ComponentPreview code={exampleCode} name="Card">
  <Example />
</ComponentPreview>

## Usage

The live example above is the complete, type-checked package example. It shows the recommended composition and representative variants or states; copy its displayed source as a starting point, then keep only the parts your interface needs.

## Import

```tsx
import { Card, CardAction, CardContent, CardDescription, CardFooter, CardHeader, CardTitle } from "@stylextras/ui/card";
```

## Anatomy

- `Card` — Styled `<div>`.
- `CardAction` — Styled `<div>`.
- `CardContent` — Styled `<div>`.
- `CardDescription` — Styled `<p>`.
- `CardFooter` — Styled `<div>`.
- `CardHeader` — Styled `<div>`.
- `CardTitle` — Styled `<h3>`.

## Accessibility and browser behavior

Each part renders and styles the native element named above. The API reference states the exact React or inherited prop base, including whether `ref` is accepted, every omitted native prop, and every component-applied default. Any native prop not named in `Omit` remains accepted, including standard ARIA/data attributes and event handlers. `sx` is composed last.

This component relies on the browser for its behavior, including keyboard, focus, form, and validation semantics where those apply. It does not ship a component behavior controller. If an optional presentation API is unavailable, the underlying native control and semantics continue to work with the browser’s ordinary presentation.

## API reference

### Card

`CardProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"div">, "className" | "style">`. The props below complete `CardProps`.

- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.
- `as` — React element type. Default: `'div'`; use a semantic element such as `article` when the card is a standalone composition.

### CardAction

`CardActionProps`

Complete native/inherited prop base: `CardProps`. The props below complete `CardActionProps`.

- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.

### CardContent

`CardContentProps`

Complete native/inherited prop base: `CardProps`. The props below complete `CardContentProps`.

- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.

### CardDescription

`CardDescriptionProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"p">, "className" | "style">`. The props below complete `CardDescriptionProps`.

- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.

### CardFooter

`CardFooterProps`

Complete native/inherited prop base: `CardProps`. The props below complete `CardFooterProps`.

- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.

### CardHeader

`CardHeaderProps`

Complete native/inherited prop base: `CardProps`. The props below complete `CardHeaderProps`.

- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.

### CardTitle

`CardTitleProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"h3">, "className" | "style">`. The props below complete `CardTitleProps`.

- `as` — React heading element type. Default: `'h3'`; choose the level that fits the surrounding document outline.
- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.
