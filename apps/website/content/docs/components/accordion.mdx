---
title: "Accordion"
description: "Exclusive disclosure built on details[name]."
---

import Example from "@stylextras/ui/accordion/example";
import exampleCode from "@stylextras/ui/accordion/example?raw";

## Examples

<ComponentPreview code={exampleCode} name="Accordion">
  <Example />
</ComponentPreview>

## Usage

The live example above is the complete, type-checked package example. It shows the recommended composition and representative variants or states; copy its displayed source as a starting point, then keep only the parts your interface needs.

## Import

```tsx
import { Accordion, AccordionContent, AccordionItem, AccordionTrigger } from "@stylextras/ui/accordion";
```

## Anatomy

- `Accordion` — Styled `<div>`.
- `AccordionContent` — Styled `<div>`.
- `AccordionItem` — Styled `<details>`.
- `AccordionTrigger` — Styled `<summary>`.
- `AccordionIndicator` — Styled default disclosure indicator.

## Accessibility and browser behavior

Each part renders and styles the native element named above. The API reference states the exact React or inherited prop base, including whether `ref` is accepted, every omitted native prop, and every component-applied default. Any native prop not named in `Omit` remains accepted, including standard ARIA/data attributes and event handlers. `sx` is composed last.

This component relies on the browser for its behavior, including keyboard, focus, form, and validation semantics where those apply. It does not ship a component behavior controller. If an optional presentation API is unavailable, the underlying native control and semantics continue to work with the browser’s ordinary presentation.

## API reference

### Accordion

`AccordionProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"div">, "className" | "style">`. The props below complete `AccordionProps`.

- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.

### AccordionContent

`AccordionContentProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"div">, "className" | "style">`. The props below complete `AccordionContentProps`.

- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.

### AccordionItem

`AccordionItemProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"details">, "className" | "style">`. The props below complete `AccordionItemProps`.

- `name` — `string`. Optional. Items sharing a name are mutually exclusive through native details behavior; omit it for independent disclosures.
- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.

### AccordionTrigger

`AccordionTriggerProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"summary">, "className" | "style">`. The props below complete `AccordionTriggerProps`.

- `indicator` — `React.ReactNode | false`. Decorative marker rendered beside the label; pass `false` to hide it. Defaults to `AccordionIndicator`.
- `indicatorPosition` — `'start' | 'end'`. Places the indicator before or after the trigger label. Default: `'end'`.
- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.

### AccordionIndicator

`AccordionIndicatorProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"span">, "children" | "className" | "style">`.

- `sx` — `stylex.StyleXStyles`. Overrides the indicator frame; the chevron remains decorative and receives `aria-hidden="true"`.
