---
title: "Combobox"
description: "Filterable input with a popover listbox."
---

import Example from "@stylextras/ui/combobox/example";

## Examples

<ComponentPreview name="Combobox">
  <Example />
</ComponentPreview>

### Example code

```tsx
'use client';

import { Field, FieldDescription, FieldLabel } from '../field';
import {
  Combobox,
  ComboboxContent,
  ComboboxEmpty,
  ComboboxInput,
  ComboboxItem,
  ComboboxList,
  ComboboxStatus,
} from './index';

const reviewers = [
  { label: 'Alex Kim', value: 'alex' },
  { label: 'Taylor Rivera', value: 'taylor' },
  { label: 'Jordan Patel', value: 'jordan' },
  { label: 'Sam Lee', value: 'sam' },
];

export default function Example() {
  return (
    <Field>
      <FieldLabel htmlFor="reviewer">Reviewer</FieldLabel>
      <Combobox name="reviewer" required>
        <ComboboxInput
          id="reviewer"
          aria-describedby="reviewer-description"
          placeholder="Search reviewers…"
        />
        <ComboboxContent>
          <ComboboxList>
            {reviewers.map((reviewer) => (
              <ComboboxItem key={reviewer.value} value={reviewer.value}>
                {reviewer.label}
              </ComboboxItem>
            ))}
          </ComboboxList>
          <ComboboxEmpty />
          <ComboboxStatus>
            {(count) => `${count} reviewer${count === 1 ? '' : 's'} available.`}
          </ComboboxStatus>
        </ComboboxContent>
      </Combobox>
      <FieldDescription id="reviewer-description">
        Use Arrow keys, Home, End, Enter, and Escape.
      </FieldDescription>
    </Field>
  );
}
```

## Import

```tsx
import { Combobox, ComboboxContent, ComboboxEmpty, ComboboxInput, ComboboxItem, ComboboxList, ComboboxStatus } from "@stylextras/ui/combobox";
```

## Anatomy

- `Combobox` — Styled `<div>`.
- `ComboboxContent` — Styled `<div>`.
- `ComboboxEmpty` — Styled `<div>`.
- `ComboboxInput` — Styled `<input>`.
- `ComboboxItem` — Styled `<div>`.
- `ComboboxList` — `<div role="listbox">` containing only options.
- `ComboboxStatus` — Visually hidden polite result announcement.

## Accessibility and browser behavior

Each part renders and styles the native element named above. The API reference states the exact React or inherited prop base, including whether `ref` is accepted, every omitted native prop, and every component-applied default. Any native prop not named in `Omit` remains accepted, including standard ARIA/data attributes and event handlers. `sx` is composed last.

Focus remains on the text/search input while Arrow keys, Home, End, and Enter
operate the DOM-ordered options through `aria-activedescendant`; options never
enter the ordinary Tab order. `aria-activedescendant` is present only while the
popover is open. Escape restores the committed label, while form submission and
reset use the committed value. Put only `ComboboxItem` options in
`ComboboxList`; `ComboboxEmpty` and `ComboboxStatus` must be siblings outside the
listbox. Duplicate option values produce a development warning and option
identity remains ID-based.

## API reference

### Combobox

`ComboboxProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"div">, "className" | "defaultValue" | "style">`. The props below complete `ComboboxProps`.

- `defaultValue` — `string`. Default: `''`.
- `disabled` — `boolean`. Default: `false`.
- `form` — `string` for associating both form controls with an external form.
- `name` — `string`.
- `onValueChange` — `(value: string) => void`.
- `required` — `boolean`. Default: `false`.
- `requiredMessage` — Localizable native validation message.
- `sx` — `stylex.StyleXStyles`.
- `value` — `string`.

### ComboboxContent

`ComboboxContentProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"div">, "className" | "id" | "popover" | "role" | "style">`. The props below complete `ComboboxContentProps`.

- `sx` — `stylex.StyleXStyles`.

### ComboboxEmpty

`ComboboxEmptyProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"div">, "className" | "style">`. The props below complete `ComboboxEmptyProps`.

- `children` — `ReactNode`. Default: `'No results.'`.
- `sx` — `stylex.StyleXStyles`.

### ComboboxInput

`ComboboxInputProps`

Complete native/inherited prop base omits the component-owned active-descendant,
listbox, value, submission, validity, disabled, role, and input-type props.
`Combobox` supplies `disabled`, `form`, and `required` consistently to the
visible input and submitted control.

- `autoComplete` — `ComboboxInputProps["autoComplete"]`. Default: `'off'`.
- `sx` — `stylex.StyleXStyles`.
- `type` — `'text' | 'search'`. Default: `'search'`.

### ComboboxItem

`ComboboxItemProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"div">, "aria-disabled" | "aria-selected" | "className" | "hidden" | "id" | "onClick" | "role" | "style">`. The props below complete `ComboboxItemProps`.

- `disabled` — `boolean`. Default: `false`.
- `onClick` — `(event: MouseEvent<HTMLDivElement>) => void`.
- `sx` — `stylex.StyleXStyles`.
- `textValue` — `string`.
- `value` — `string`. Required.

### ComboboxStatus

- `children` — `ReactNode | ((count: number) => ReactNode)`. Required.
- `delay` — Debounce delay in milliseconds. Default: `150`.
- `sx` — `stylex.StyleXStyles`.

### ComboboxList

- Native `div` props, a forwarded ref, and `sx`.
- Owns the generated listbox ID referenced by `ComboboxInput`.
