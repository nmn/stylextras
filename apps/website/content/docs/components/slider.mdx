---
title: "Slider"
description: "Native range input composition."
---

import Example from "@stylextras/ui/slider/example";
import exampleCode from "@stylextras/ui/slider/example?raw";

## Examples

<ComponentPreview code={exampleCode} name="Slider">
  <Example />
</ComponentPreview>

## Usage

The live example above is the complete, type-checked package example. It shows the recommended composition and representative variants or states; copy its displayed source as a starting point, then keep only the parts your interface needs.

## Import

```tsx
import { Slider } from "@stylextras/ui/slider";
```

## Anatomy

- `Slider` — Styled `<input>`.

## Accessibility and browser behavior

Each part renders and styles the native element named above. The API reference states the exact React or inherited prop base, including whether `ref` is accepted, every omitted native prop, and every component-applied default. Any native prop not named in `Omit` remains accepted, including standard ARIA/data attributes and event handlers. `sx` is composed last.

This component uses a focused client controller for composite state and keyboard interaction while retaining native elements for focus and form semantics where possible. Its behavior comes from the component controller rather than a browser-feature polyfill.

## API reference

### Slider

`SliderProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"input">, "className" | "style" | "type">`. The props below complete `SliderProps`.

- `max` — `SliderProps["max"]`. Inclusive upper bound for the native range input. Default: `100`.
- `min` — `SliderProps["min"]`. Inclusive lower bound for the native range input. Default: `0`.
- `step` — `SliderProps["step"]`. Sets the permitted increment between numeric values. Default: `1`.
- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.
