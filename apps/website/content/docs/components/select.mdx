---
title: "Select"
description: "Native select with customizable-select CSS."
---

import Example from "@stylextras/ui/select/example";
import exampleCode from "@stylextras/ui/select/example?raw";

## Examples

<ComponentPreview code={exampleCode} name="Select">
  <Example />
</ComponentPreview>

## Usage

The live example above is the complete, type-checked package example. It shows the recommended composition and representative variants or states; copy its displayed source as a starting point, then keep only the parts your interface needs.

## Import

```tsx
import { Option, Select } from "@stylextras/ui/select";
```

## Anatomy

- `Select` — Styled `<select>`.
- `Option` — Styled `<option>`.

## Accessibility and browser behavior

Each part renders and styles the native element named above. The API reference states the exact React or inherited prop base, including whether `ref` is accepted, every omitted native prop, and every component-applied default. Any native prop not named in `Omit` remains accepted, including standard ARIA/data attributes and event handlers. `sx` is composed last.

This component starts with semantic HTML and enhances it with newer browser APIs when they are available. If an optional presentation API is unavailable, the underlying native control and semantics continue to work with the browser’s ordinary presentation.

## API reference

### Select

`SelectProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"select">, "className" | "style">`. The props below complete `SelectProps`.

- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.

### Option

`OptionProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"option">, "className" | "style">`. The props below complete `OptionProps`.

- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.
