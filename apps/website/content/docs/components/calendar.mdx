---
title: "Calendar"
description: "Intl-backed calendar grid."
---

import Example from "@stylextras/ui/calendar/example";
import exampleCode from "@stylextras/ui/calendar/example?raw";

## Examples

<ComponentPreview code={exampleCode} name="Calendar">
  <Example />
</ComponentPreview>

## Usage

The live example above is the complete, type-checked package example. It shows the recommended composition and representative variants or states; copy its displayed source as a starting point, then keep only the parts your interface needs.

## Import

```tsx
import { Calendar } from "@stylextras/ui/calendar";
```

## Anatomy

- `Calendar` — Root `<div>` with month-navigation buttons and an ARIA grid.
- Each date — `role="gridcell"` wrapper containing a native `<button type="button">`.

## Accessibility and browser behavior

Each part renders and styles the native element named above. The API reference states the exact React or inherited prop base, including whether `ref` is accepted, every omitted native prop, and every component-applied default. Any native prop not named in `Omit` remains accepted, including standard ARIA/data attributes and event handlers. `sx` is composed last.

This component uses a focused client controller for composite state and keyboard interaction while retaining native elements for focus and form semantics where possible. Its behavior comes from the component controller rather than a browser-feature polyfill.

Every date button is named with its full localized date. Arrow keys respect text direction; Page Up/Down preserves the day where possible, and unavailable month buttons are disabled at `min`/`max` boundaries. Month-button navigation moves and clamps the grid's active date as well as the visible month, so exactly one enabled date remains in the roving tab sequence.

Bounds are parsed once. Malformed `min` or `max` values are ignored. When both
are valid but `min` is after `max`, both are ignored and a development warning
is emitted, ensuring the grid always retains one usable roving tab stop.

Pass `today` as an ISO `YYYY-MM-DD` value when the server already knows the user's local date. Without a selected value or `today`, the component renders its localized loading status consistently on the server and resolves the browser-local date after hydration, avoiding timezone-dependent hydration mismatches.

## API reference

### Calendar

`CalendarProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"div">, "className" | "style">`. The props below complete `CalendarProps`.

- `defaultValue` — ISO `YYYY-MM-DD` string. Initial selected date for uncontrolled usage. Default: `''`.
- `loadingLabel` — `string`. Accessible progress label announced while calendar data is pending. Default: `'Loading calendar'`.
- `locale` — `string`. BCP 47 locale used for month labels, dates, and the derived first weekday. Default: `'en-US'`.
- `max` — `string`. Inclusive upper bound in the native control's canonical format.
- `min` — `string`. Inclusive lower bound in the native control's canonical format.
- `nextMonthLabel` — `string`. Accessible name for the next-month button. Default: `'Next month'`.
- `onValueChange` — `(value: string) => void`. Called when user interaction requests a new value.
- `previousMonthLabel` — `string`. Accessible name for the previous-month button. Default: `'Previous month'`.
- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.
- `today` — `string`. Stable local date in `YYYY-MM-DD` form for server rendering and `aria-current="date"`.
- `value` — `string`. Current controlled value; pair it with `onValueChange`.
- `weekStartsOn` — `number`. Zero-based weekday index used as the first grid column. Default: `getWeekStartsOn(locale)`.
