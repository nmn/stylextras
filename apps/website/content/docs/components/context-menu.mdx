---
title: "ContextMenu"
description: "Popover menu opened from a context gesture."
---

import Example from "@stylextras/ui/context-menu/example";
import exampleCode from "@stylextras/ui/context-menu/example?raw";

## Examples

<ComponentPreview code={exampleCode} name="ContextMenu">
  <Example />
</ComponentPreview>

## Usage

The live example above is the complete, type-checked package example. It shows the recommended composition and representative variants or states; copy its displayed source as a starting point, then keep only the parts your interface needs.

## Import

```tsx
import {
  ContextMenu,
  ContextMenuButton,
  ContextMenuGroup,
  ContextMenuItem,
  ContextMenuLabel,
  ContextMenuSeparator,
  ContextMenuTrigger,
} from "@stylextras/ui/context-menu";
```

## Anatomy

- `ContextMenu` — Styled `<div>`.
- `ContextMenuButton` — Visible `<button>` alternative to the context gesture.
- `ContextMenuGroup` — `<div role="group">` named by a menu label.
- `ContextMenuItem` — Styled `<button>`.
- `ContextMenuLabel` — Styled `<div>`.
- `ContextMenuSeparator` — Styled `<hr>`.
- `ContextMenuTrigger` — Focusable, named `<div role="group">` context-gesture region.

## Accessibility and browser behavior

Give both the trigger region and menu an accessible name. Pointer users can right-click or long-press the region; keyboard users can press the Context Menu key or Shift+F10. Always render `ContextMenuButton` as the visible keyboard and touch alternative when the context action is not available elsewhere.

Disabled items remain arrow-key focusable so their labels are discoverable, but cannot be activated. Escape closes the menu and restores focus to its invoker. `sx` is composed last.

Wrap related items and their visible label in `ContextMenuGroup`, and connect
the group to `ContextMenuLabel` with `aria-labelledby`.

This component starts with semantic HTML and enhances it with newer browser APIs when they are available. When the relevant browser interaction API is unavailable, the package loads a narrowly scoped, feature-detected bridge for that behavior.

## API reference

### ContextMenu

`ContextMenuProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"div">, "aria-label" | "aria-labelledby" | "className" | "popover" | "role" | "style">`. The props below complete `ContextMenuProps`.

- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.
- One of `aria-label` or `aria-labelledby` is required.

### ContextMenuButton

`ContextMenuButtonProps`

Complete inherited prop base: `AccessibleButtonPropsWithout<"aria-controls" | "aria-haspopup" | "popoverTarget" | "popoverTargetAction">`.

- `target` — `string`. ID of the element controlled by this trigger. Required.
- `size` — Accessible `ButtonSize`. Controls visual dimensions and, for interactive components, the target size. Default: `'md'`; icon sizes require `aria-label` or `aria-labelledby`.
- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.
- `type` — Native button type. Sets the underlying element's native control type. Default: `'button'`.
- `variant` — `ButtonVariant`. Selects the built-in visual treatment. Default: `'outline'`.
- Icon sizes require `aria-label` or `aria-labelledby`.

### ContextMenuItem

`ContextMenuItemProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"button">, "className" | "role" | "style">`. The props below complete `ContextMenuItemProps`.

- `disabled` — `boolean`. Default: `false`. Rendered as `aria-disabled` so the item remains discoverable.
- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.
- `type` — `ContextMenuItemProps["type"]`. Sets the underlying element's native control type. Default: `'button'`.

### ContextMenuGroup

`ContextMenuGroupProps` extends native `div` props with a forwarded ref and `sx`.

- `aria-labelledby` — Required ID reference to the group's `ContextMenuLabel`.
- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.

### ContextMenuLabel

`ContextMenuLabelProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"div">, "className" | "style">`. The props below complete `ContextMenuLabelProps`.

- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.

### ContextMenuSeparator

`ContextMenuSeparatorProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"hr">, "className" | "style">`. The props below complete `ContextMenuSeparatorProps`.

- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.

### ContextMenuTrigger

`ContextMenuTriggerProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"div">, "aria-label" | "aria-labelledby" | "className" | "role" | "style">`. The props below complete `ContextMenuTriggerProps`.

- `aria-controls` — Identifies the context-menu popover referenced by `target`.
- `aria-haspopup` — `ContextMenuTriggerProps["aria-haspopup"]`. Announces the kind of popup controlled by the trigger. Default: `'menu'`.
- `role` — Fixed to `'group'` so the focusable context-menu area has group semantics.
- `tabIndex` — Adds the context-menu area to the ordinary Tab order. Default: `0`.
- One of `aria-label` or `aria-labelledby` is required.
- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.
- `target` — `string`. ID of the element controlled by this trigger. Required.
