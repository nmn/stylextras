---
title: "AlertDialog"
description: "Modal confirmation dialog."
---

import Example from "@stylextras/ui/alert-dialog/example";
import exampleCode from "@stylextras/ui/alert-dialog/example?raw";

## Examples

<ComponentPreview code={exampleCode} name="AlertDialog">
  <Example />
</ComponentPreview>

## Usage

The live example above is the complete, type-checked package example. It shows the recommended composition and representative variants or states; copy its displayed source as a starting point, then keep only the parts your interface needs.

## Import

```tsx
import { AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle, AlertDialogTrigger } from "@stylextras/ui/alert-dialog";
import { DialogCommandBridge } from "@stylextras/ui/dialog/client";
```

## Anatomy

- `AlertDialog` — Styled `<dialog>`.
- `AlertDialogAction` — Styled `<button>`.
- `AlertDialogCancel` — Styled `<button>`.
- `AlertDialogDescription` — Styled `<p>`.
- `AlertDialogFooter` — Styled `<footer>`.
- `AlertDialogHeader` — Styled `<header>`.
- `AlertDialogTitle` — Styled `<h2>`.
- `AlertDialogTrigger` — Styled `<button>`.
- `DialogCommandBridge` — Optional feature-detected invoker-command fallback.

## Accessibility and browser behavior

Each part renders and styles the native element named above. The API reference states the exact React or inherited prop base, including whether `ref` is accepted, every omitted native prop, and every component-applied default. Any native prop not named in `Omit` remains accepted, including standard ARIA/data attributes and event handlers. `sx` is composed last.

`AlertDialog` requires both an accessible name and a programmatic description.
Initial focus should land on the least destructive action, normally the cancel
button. The example opts into `DialogCommandBridge`; it feature-detects native
invoker-command support and only then loads the fallback. `LazyAlertDialog`
installs that fallback internally.

## API reference

### AlertDialog

`AlertDialogProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"dialog">, "aria-label" | "aria-labelledby" | "className" | "role" | "style">`, plus a required accessible name and `aria-describedby`.

- `aria-label` or `aria-labelledby` — Required accessible name.
- `aria-describedby` — Required description ID reference.
- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.

### AlertDialogAction

`AlertDialogActionProps`

Complete native/inherited prop base: `ButtonProps`. The props below complete `AlertDialogActionProps`.

- `size` — `ButtonSize`. Controls visual dimensions and, for interactive components, the target size. Default: `'md'`.
- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.
- `target` — `string`. ID of the element controlled by this trigger. Required.
- `type` — `AlertDialogActionProps["type"]`. Sets the underlying element's native control type. Default: `'button'`.
- `variant` — `ButtonVariant`. Selects the built-in visual treatment. Default: `'danger'`.

### AlertDialogCancel

`AlertDialogCancelProps`

Complete native/inherited prop base: `ButtonProps`. The props below complete `AlertDialogCancelProps`.

- `size` — `ButtonSize`. Controls visual dimensions and, for interactive components, the target size. Default: `'md'`.
- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.
- `target` — `string`. ID of the element controlled by this trigger. Required.
- `type` — `AlertDialogCancelProps["type"]`. Sets the underlying element's native control type. Default: `'button'`.
- `variant` — `ButtonVariant`. Selects the built-in visual treatment. Default: `'outline'`.

### AlertDialogDescription

`AlertDialogDescriptionProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"p">, "className" | "style">`. The props below complete `AlertDialogDescriptionProps`.

- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.

### AlertDialogFooter

`AlertDialogFooterProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"footer">, "className" | "style">`. The props below complete `AlertDialogFooterProps`.

- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.

### AlertDialogHeader

`AlertDialogHeaderProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"header">, "className" | "style">`. The props below complete `AlertDialogHeaderProps`.

- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.

### AlertDialogTitle

`AlertDialogTitleProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"h2">, "className" | "style">`. The props below complete `AlertDialogTitleProps`.

- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.

### AlertDialogTrigger

`AlertDialogTriggerProps`

Complete native/inherited prop base: `ButtonProps`. The props below complete `AlertDialogTriggerProps`.

- `aria-haspopup` — `AlertDialogTriggerProps["aria-haspopup"]`. Announces the kind of popup controlled by the trigger. Default: `'dialog'`.
- `size` — `ButtonSize`. Controls visual dimensions and, for interactive components, the target size. Default: `'md'`.
- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.
- `target` — `string`. ID of the element controlled by this trigger. Required.
- `type` — `AlertDialogTriggerProps["type"]`. Sets the underlying element's native control type. Default: `'button'`.
- `variant` — `ButtonVariant`. Selects the built-in visual treatment. Default: `'primary'`.
