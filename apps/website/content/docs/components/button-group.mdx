---
title: "ButtonGroup"
description: "Grouped toolbar and equal-width action layouts."
---

import Example from "@stylextras/ui/button-group/example";
import exampleCode from "@stylextras/ui/button-group/example?raw";

## Examples

<ComponentPreview code={exampleCode} name="ButtonGroup">
  <Example />
</ComponentPreview>

## Usage

The live example above is the complete, type-checked package example. It shows the recommended composition and representative variants or states; copy its displayed source as a starting point, then keep only the parts your interface needs.

## Import

```tsx
import { ButtonGroup } from "@stylextras/ui/button-group";
```

## Anatomy

- `ButtonGroup` — Styled `<div>`.

## Accessibility and browser behavior

Each part renders and styles the native element named above. The API reference states the exact React or inherited prop base, including whether `ref` is accepted, every omitted native prop, and every component-applied default. Any native prop not named in `Omit` remains accepted, including standard ARIA/data attributes and event handlers. `sx` is composed last.

This component starts with semantic HTML and enhances it with newer browser APIs when they are available. When the relevant browser interaction API is unavailable, the package loads a narrowly scoped, feature-detected bridge for that behavior.

## API reference

### ButtonGroup

`ButtonGroupProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"div">, "aria-label" | "aria-labelledby" | "className" | "role" | "style">` plus one required accessible name.

- `aria-label` or `aria-labelledby` — Required toolbar/group name.
- `orientation` — `"vertical" | "horizontal"`. Controls the layout axis and, where applicable, Arrow-key behavior. Default: `'horizontal'`.
- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.
- `variant` — `"toolbar" | "actions"`. Selects the built-in visual treatment. Default: `'toolbar'`.
