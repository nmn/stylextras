---
title: "Item"
description: "Reusable content row composition."
---

import Example from "@stylextras/ui/item/example";
import exampleCode from "@stylextras/ui/item/example?raw";

## Examples

<ComponentPreview code={exampleCode} name="Item">
  <Example />
</ComponentPreview>

## Usage

The live example above is the complete, type-checked package example. It shows the recommended composition and representative variants or states; copy its displayed source as a starting point, then keep only the parts your interface needs.

## Import

```tsx
import { Item, ItemActions, ItemContent, ItemDescription, ItemMedia, ItemTitle } from "@stylextras/ui/item";
```

## Anatomy

- `Item` — Styled `<div>`.
- `ItemActions` — Styled `<div>`.
- `ItemContent` — Styled `<div>`.
- `ItemDescription` — Styled `<p>`.
- `ItemMedia` — Styled `<div>`.
- `ItemTitle` — Styled `<h3>`.

## Accessibility and browser behavior

Each part renders and styles the native element named above. The API reference states the exact React or inherited prop base, including whether `ref` is accepted, every omitted native prop, and every component-applied default. Any native prop not named in `Omit` remains accepted, including standard ARIA/data attributes and event handlers. `sx` is composed last.

This component relies on the browser for its behavior, including keyboard, focus, form, and validation semantics where those apply. It does not ship a component behavior controller. If an optional presentation API is unavailable, the underlying native control and semantics continue to work with the browser’s ordinary presentation.

## API reference

### Item

`ItemProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"div">, "className" | "style">`. The props below complete `ItemProps`.

- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.
- `as` — React element type. Default: `'div'`; choose a semantic container appropriate to the item.

### ItemActions

`ItemActionsProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"div">, "className" | "style">`. The props below complete `ItemActionsProps`.

- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.

### ItemContent

`ItemContentProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"div">, "className" | "style">`. The props below complete `ItemContentProps`.

- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.

### ItemDescription

`ItemDescriptionProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"p">, "className" | "style">`. The props below complete `ItemDescriptionProps`.

- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.

### ItemMedia

`ItemMediaProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"div">, "className" | "style">`. The props below complete `ItemMediaProps`.

- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.

### ItemTitle

`ItemTitleProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"h3">, "className" | "style">`. The props below complete `ItemTitleProps`.

- `as` — React heading element type. Default: `'h3'`; match the surrounding document outline.
- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.
