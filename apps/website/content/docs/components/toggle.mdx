---
title: "Toggle"
description: "Native pressed button."
---

import Example from "@stylextras/ui/toggle/example";
import exampleCode from "@stylextras/ui/toggle/example?raw";

## Examples

<ComponentPreview code={exampleCode} name="Toggle">
  <Example />
</ComponentPreview>

## Usage

The live example above is the complete, type-checked package example. It shows the recommended composition and representative variants or states; copy its displayed source as a starting point, then keep only the parts your interface needs.

## Import

```tsx
import { Toggle } from "@stylextras/ui/toggle";
```

## Anatomy

- `Toggle` — Styled `<button>`.

## Accessibility and browser behavior

Each part renders and styles the native element named above. The API reference states the exact React or inherited prop base, including whether `ref` is accepted, every omitted native prop, and every component-applied default. Any native prop not named in `Omit` remains accepted, including standard ARIA/data attributes and event handlers. `sx` is composed last.

This component relies on the browser for its behavior, including keyboard, focus, form, and validation semantics where those apply. It does not ship a component behavior controller. If an optional presentation API is unavailable, the underlying native control and semantics continue to work with the browser’s ordinary presentation.

## API reference

### Toggle

`ToggleProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"button">, "className" | "style">`. The props below complete `ToggleProps`.

- `aria-pressed` — `boolean | 'false' | 'mixed' | 'true'`. Required controlled pressed state; use `'mixed'` only for an indeterminate aggregate.
- `size` — `ToggleSize`. Controls visual dimensions and, for interactive components, the target size. Default: `'md'`.
- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.
- `type` — `ToggleProps["type"]`. Sets the underlying element's native control type. Default: `'button'`.
