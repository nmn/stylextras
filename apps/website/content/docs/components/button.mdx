---
title: "Button"
description: "Styled native button including icon sizes."
---

import Example from "@stylextras/ui/button/example";
import exampleCode from "@stylextras/ui/button/example?raw";

## Examples

<ComponentPreview code={exampleCode} name="Button">
  <Example />
</ComponentPreview>

## Usage

### Basic

```tsx
import { Button } from "@stylextras/ui/button";

export function SaveButton() {
  return <Button>Save changes</Button>;
}
```

### Icon button

Icon-only buttons need an accessible name.

```tsx
import { Button } from "@stylextras/ui/button";

export function AddButton() {
  return (
    <Button size="icon" aria-label="Add item">
      +
    </Button>
  );
}
```

### Link action

`ButtonLink` keeps native anchor behavior while sharing the button variants and
size contract.

```tsx
import { ButtonLink } from "@stylextras/ui/button";

export function BrowseComponentsLink() {
  return <ButtonLink href="/docs">Browse components</ButtonLink>;
}
```

## Import

```tsx
import { Button, ButtonLink } from "@stylextras/ui/button";
```

## Anatomy

- `Button` — Styled `<button>`.
- `ButtonLink` — Styled `<a>` with the same variants and sizes.

## Accessibility and browser behavior

Each part renders and styles the native element named above. The API reference states the exact React or inherited prop base, including whether `ref` is accepted, every omitted native prop, and every component-applied default. Any native prop not named in `Omit` remains accepted, including standard ARIA/data attributes and event handlers. `sx` is composed last.

This component relies on the browser for its behavior, including keyboard, focus, form, and validation semantics where those apply. It does not ship a component behavior controller. `icon`, `icon-sm`, and `icon-lg` are a separate prop-union branch that requires `aria-label` or `aria-labelledby`; text sizes do not require a redundant ARIA name. Button-based wrappers preserve this contract with the exported distributive `AccessibleButtonPropsWithout` helper.

## API reference

### Button

`ButtonProps`

Complete native/inherited prop base: `Omit<React.ComponentPropsWithRef<"button">, "className" | "size" | "style">`, combined as the size-discriminated `AccessibleButtonProps` union. Icon-size branches require `AccessibleIconButtonName`.

- `size` — `ButtonSize`. Controls visual dimensions and, for interactive components, the target size. Default: `'md'`.
- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.
- `type` — `ButtonProps["type"]`. Sets the underlying element's native control type. Default: `'button'`.
- `variant` — `ButtonVariant`. Selects the built-in visual treatment. Default: `'primary'`.

### ButtonLink

`ButtonLinkProps`

Complete native/inherited prop base:
`Omit<React.ComponentPropsWithRef<"a">, "className" | "href" | "style">`, combined as
the size-discriminated `AccessibleButtonLinkProps` union.

- `href` — `string`. Required real navigation destination.
- `size` — `ButtonSize`. Controls visual dimensions and, for interactive components, the target size. Default: `'md'`.
- `sx` — `stylex.StyleXStyles`. Theme-aware local overrides composed after the built-in styles.
- `variant` — `ButtonVariant`. Selects the built-in visual treatment. Default: `'primary'`.
