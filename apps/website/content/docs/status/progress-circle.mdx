---
title: Progress Circle
description: "Progress Circle provides a status primitive for feedback surfaces."
---

import { lazy, Suspense } from "react";

export const Example = lazy(
  () => import("@stylextras/ui/progress-circle/example"),
);

# Progress Circle

Use Progress Circle to communicate state, progress, or feedback in a compact token-driven surface.

The implementation is intentionally small: visual defaults come from token variables, and the public props keep native HTML attributes available where the underlying element supports them.

## Import

```tsx
import { ProgressCircle } from "@stylextras/ui/progress-circle";
```

## Basic usage

Start here when wiring the component into an interface. The demo shows the expected behavior, and the code block shows the smallest useful invocation.

<Suspense fallback={null}>
  <Example />
</Suspense>

```tsx
import { ProgressCircle } from "@stylextras/ui/progress-circle";

<ProgressCircle aria-label="Sync progress" value={72} max={100} showValue />;
```

## Props

The public prop types are exported from `@stylextras/ui/progress-circle`. Most components also accept an `sx` prop for custom styles and forward safe native HTML attributes for the underlying element.

```tsx
export type ProgressCircleProps = Omit<
  BaseProps,
  "aria-label" | "aria-labelledby" | "className" | "style"
> &
  AccessibleAriaNameProps & {
    max?: number;
    showValue?: boolean;
    size?: ProgressCircleSize;
    sx?: StyleXStyles;
    value?: number;
  };
```

## Customize styles

Use the `sx` prop to compose custom styles with the component's token-driven defaults.

```tsx
import * as stylex from "@stylexjs/stylex";
import { colors } from "@stylextras/ui/tokens/color";
import { spacing } from "@stylextras/ui/tokens/spacing";
import { ProgressCircle } from "@stylextras/ui/progress-circle";

<ProgressCircle aria-label="Example" sx={styles.emphasis} />;

const styles = stylex.create({
  emphasis: {
    backgroundColor: colors.bgSubtle,
    padding: spacing.md,
  },
});
```

## Complete example

This example shows the component in a fuller composition with the surrounding pieces it usually needs.

```tsx
import { ProgressCircle } from "@stylextras/ui/progress-circle";

function ProgressCircleExample() {
  return (
    <>
      <div>
        <ProgressCircle
          aria-label="Small progress"
          value={24}
          max={100}
          showValue
          size="sm"
        />
        <ProgressCircle
          aria-label="Medium progress"
          value={58}
          max={100}
          showValue
          size="md"
        />
        <ProgressCircle
          aria-label="Large progress"
          value={92}
          max={100}
          showValue
          size="lg"
        />
      </div>
    </>
  );
}
```

## Accessibility notes

Make status text explicit. Use live regions only for updates that need to be announced immediately.

## Implementation notes

- Styles use token variables from `@stylextras/ui/tokens/*`.
- The implementation lives in `packages/ui/src/progress-circle/index.tsx`.
