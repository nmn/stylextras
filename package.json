{
  "name": "stylextras",
  "devDependencies": {
    "@types/bun": "^1.2.3",
    "vitest": "^3.0.6",
    "typescript": "^5.7.3",
    "@biomejs/biome": "^1.9.4"
  },
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun run --filter '*' test",
    "format": "biome format --write ."
  },
  "workspaces": [
    "apps/*",
    "tools/*",
    "packages/*"
  ]
}
