import * as stylex from "@stylexjs/stylex";

export const gridAutoFlow = stylex.create({
  column: { gridAutoFlow: "column" },
  column_dense: { gridAutoFlow: "column dense" },
  row: { gridAutoFlow: "row" },
  row_dense: { gridAutoFlow: "row dense" },
});

